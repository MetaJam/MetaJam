
# This file is automatically generated, you probably don't want to edit this

metaIncOptions <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "metaIncOptions",
    inherit = jmvcore::Options,
    public = list(
        initialize = function(
            studyLabel = NULL,
            eventE = NULL,
            timeE = NULL,
            eventC = NULL,
            timeC = NULL,
            sm = "IRR",
            method = "Inverse",
            correctionMethod = "only0",
            incr = 0.5,
            model = "both",
            methodTau = "REML",
            methodRandomCi = "classic",
            prediction = FALSE,
            confidenceLevel = 95,
            summary = TRUE, ...) {

            super$initialize(
                package="MetaJam",
                name="metaInc",
                requiresData=TRUE,
                ...)

            private$..studyLabel <- jmvcore::OptionVariable$new(
                "studyLabel",
                studyLabel,
                suggested=list(
                    "nominal",
                    "id"),
                permitted=list(
                    "factor",
                    "id"))
            private$..eventE <- jmvcore::OptionVariable$new(
                "eventE",
                eventE,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..timeE <- jmvcore::OptionVariable$new(
                "timeE",
                timeE,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..eventC <- jmvcore::OptionVariable$new(
                "eventC",
                eventC,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..timeC <- jmvcore::OptionVariable$new(
                "timeC",
                timeC,
                suggested=list(
                    "continuous"),
                permitted=list(
                    "numeric"))
            private$..sm <- jmvcore::OptionList$new(
                "sm",
                sm,
                options=list(
                    "IRR",
                    "IRD",
                    "VE"),
                default="IRR")
            private$..method <- jmvcore::OptionList$new(
                "method",
                method,
                options=list(
                    "Inverse",
                    "MH"),
                default="Inverse")
            private$..correctionMethod <- jmvcore::OptionList$new(
                "correctionMethod",
                correctionMethod,
                options=list(
                    "none",
                    "only0",
                    "if0all",
                    "all"),
                default="only0")
            private$..incr <- jmvcore::OptionNumber$new(
                "incr",
                incr,
                min=0,
                default=0.5)
            private$..model <- jmvcore::OptionList$new(
                "model",
                model,
                options=list(
                    "both",
                    "random",
                    "common"),
                default="both")
            private$..methodTau <- jmvcore::OptionList$new(
                "methodTau",
                methodTau,
                options=list(
                    "REML",
                    "PM",
                    "DL",
                    "ML",
                    "HS",
                    "SJ",
                    "HE",
                    "EB"),
                default="REML")
            private$..methodRandomCi <- jmvcore::OptionList$new(
                "methodRandomCi",
                methodRandomCi,
                options=list(
                    "classic",
                    "HK"),
                default="classic")
            private$..prediction <- jmvcore::OptionBool$new(
                "prediction",
                prediction,
                default=FALSE)
            private$..confidenceLevel <- jmvcore::OptionNumber$new(
                "confidenceLevel",
                confidenceLevel,
                min=50,
                max=99.9,
                default=95)
            private$..summary <- jmvcore::OptionBool$new(
                "summary",
                summary,
                default=TRUE)

            self$.addOption(private$..studyLabel)
            self$.addOption(private$..eventE)
            self$.addOption(private$..timeE)
            self$.addOption(private$..eventC)
            self$.addOption(private$..timeC)
            self$.addOption(private$..sm)
            self$.addOption(private$..method)
            self$.addOption(private$..correctionMethod)
            self$.addOption(private$..incr)
            self$.addOption(private$..model)
            self$.addOption(private$..methodTau)
            self$.addOption(private$..methodRandomCi)
            self$.addOption(private$..prediction)
            self$.addOption(private$..confidenceLevel)
            self$.addOption(private$..summary)
        }),
    active = list(
        studyLabel = function() private$..studyLabel$value,
        eventE = function() private$..eventE$value,
        timeE = function() private$..timeE$value,
        eventC = function() private$..eventC$value,
        timeC = function() private$..timeC$value,
        sm = function() private$..sm$value,
        method = function() private$..method$value,
        correctionMethod = function() private$..correctionMethod$value,
        incr = function() private$..incr$value,
        model = function() private$..model$value,
        methodTau = function() private$..methodTau$value,
        methodRandomCi = function() private$..methodRandomCi$value,
        prediction = function() private$..prediction$value,
        confidenceLevel = function() private$..confidenceLevel$value,
        summary = function() private$..summary$value),
    private = list(
        ..studyLabel = NA,
        ..eventE = NA,
        ..timeE = NA,
        ..eventC = NA,
        ..timeC = NA,
        ..sm = NA,
        ..method = NA,
        ..correctionMethod = NA,
        ..incr = NA,
        ..model = NA,
        ..methodTau = NA,
        ..methodRandomCi = NA,
        ..prediction = NA,
        ..confidenceLevel = NA,
        ..summary = NA)
)

metaIncResults <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "metaIncResults",
    inherit = jmvcore::Group,
    active = list(
        text = function() private$.items[["text"]]),
    private = list(),
    public=list(
        initialize=function(options) {
            super$initialize(
                options=options,
                name="",
                title="Incidence Rate Outcomes Meta-Analysis")
            self$add(jmvcore::Html$new(
                options=options,
                name="text",
                visible="(summary)",
                clearWith=list(
                    "studyLabel",
                    "eventE",
                    "timeE",
                    "eventC",
                    "timeC",
                    "sm",
                    "method",
                    "correctionMethod",
                    "incr",
                    "model",
                    "methodTau",
                    "methodRandomCi",
                    "prediction",
                    "confidenceLevel"),
                refs=list(
                    "MetaJam",
                    "metaPackage")))}))

metaIncBase <- if (requireNamespace("jmvcore", quietly=TRUE)) R6::R6Class(
    "metaIncBase",
    inherit = jmvcore::Analysis,
    public = list(
        initialize = function(options, data=NULL, datasetId="", analysisId="", revision=0) {
            super$initialize(
                package = "MetaJam",
                name = "metaInc",
                version = c(1,0,0),
                options = options,
                results = metaIncResults$new(options=options),
                data = data,
                datasetId = datasetId,
                analysisId = analysisId,
                revision = revision,
                pause = NULL,
                completeWhenFilled = FALSE,
                requiresMissings = FALSE,
                weightsSupport = 'auto')
        }))

#' Incidence Rate Outcomes Meta-Analysis
#'
#' 
#' @param data .
#' @param studyLabel .
#' @param eventE .
#' @param timeE .
#' @param eventC .
#' @param timeC .
#' @param sm .
#' @param method .
#' @param correctionMethod .
#' @param incr .
#' @param model .
#' @param methodTau .
#' @param methodRandomCi .
#' @param prediction .
#' @param confidenceLevel .
#' @param summary .
#' @return A results object containing:
#' \tabular{llllll}{
#'   \code{results$text} \tab \tab \tab \tab \tab a html \cr
#' }
#'
#' @export
metaInc <- function(
    data,
    studyLabel,
    eventE,
    timeE,
    eventC,
    timeC,
    sm = "IRR",
    method = "Inverse",
    correctionMethod = "only0",
    incr = 0.5,
    model = "both",
    methodTau = "REML",
    methodRandomCi = "classic",
    prediction = FALSE,
    confidenceLevel = 95,
    summary = TRUE) {

    if ( ! requireNamespace("jmvcore", quietly=TRUE))
        stop("metaInc requires jmvcore to be installed (restart may be required)")

    if ( ! missing(studyLabel)) studyLabel <- jmvcore::resolveQuo(jmvcore::enquo(studyLabel))
    if ( ! missing(eventE)) eventE <- jmvcore::resolveQuo(jmvcore::enquo(eventE))
    if ( ! missing(timeE)) timeE <- jmvcore::resolveQuo(jmvcore::enquo(timeE))
    if ( ! missing(eventC)) eventC <- jmvcore::resolveQuo(jmvcore::enquo(eventC))
    if ( ! missing(timeC)) timeC <- jmvcore::resolveQuo(jmvcore::enquo(timeC))
    if (missing(data))
        data <- jmvcore::marshalData(
            parent.frame(),
            `if`( ! missing(studyLabel), studyLabel, NULL),
            `if`( ! missing(eventE), eventE, NULL),
            `if`( ! missing(timeE), timeE, NULL),
            `if`( ! missing(eventC), eventC, NULL),
            `if`( ! missing(timeC), timeC, NULL))


    options <- metaIncOptions$new(
        studyLabel = studyLabel,
        eventE = eventE,
        timeE = timeE,
        eventC = eventC,
        timeC = timeC,
        sm = sm,
        method = method,
        correctionMethod = correctionMethod,
        incr = incr,
        model = model,
        methodTau = methodTau,
        methodRandomCi = methodRandomCi,
        prediction = prediction,
        confidenceLevel = confidenceLevel,
        summary = summary)

    analysis <- metaIncClass$new(
        options = options,
        data = data)

    analysis$run()

    analysis$results
}

